package com.example.lostfound;

/**
 * Server configuration.
 * Change SERVER_IP to your computer's IP address when testing on a real phone.
 *
 * - For emulator: use "10.0.2.2"
 * - For real phone: use your computer's IP (e.g., "192.168.1.100")
 *   Find it by running 'ipconfig' in Command Prompt
 */
public class ServerConfig {
    // Change this to your computer's IP when using a real phone
    public static final String SERVER_IP = "192.168.1.105";
    public static final int SERVER_PORT = 12345;
}
